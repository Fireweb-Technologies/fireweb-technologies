import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Fireweb Technologies - Web Development & Digital Marketing Agency",
  description:
    "Professional web development and digital marketing services. We create stunning websites and drive digital growth for businesses worldwide.",
  keywords:
    "web development, digital marketing, SEO, website design, e-commerce, mobile apps",
  authors: [{ name: "Fireweb Technologies" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
